node('mac') {

    stage('cleanup') {
        deleteDir()
    }

    stage('Checkout') {
        def scmInfo = checkout scm

        scmInfo.each { k,v ->
            echo "key: ${k}, value: ${v}"
        }

        def gitUrl = scmInfo.GIT_URL

        echo 'Git URL: $gitUrl'
    }
}