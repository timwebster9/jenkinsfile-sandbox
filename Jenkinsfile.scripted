node('mac') {

    stage('cleanup') {
        deleteDir()
    }

    stage('Checkout') {
        checkout scm
    }

    stage('One') {
        try {
            sh 'mvn clean install'
        } catch (err) {
            currentBuild.result = "UNSTABLE"
            echo "${err.getMessage()}"
        }
    }
    stage('Two') {
        echo 'hello world'
    }
}
