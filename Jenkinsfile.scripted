node('mac') {

    stage('cleanup') {
        deleteDir()
    }

    stage('Checkout') {
        checkout scm
    }

    stage('One') {
        try {
            throw new RuntimeException()
        } catch (err) {
            currentBuild.result = "UNSTABLE"
            echo "${err.getMessage()}"
        }
    }
    stage('Two') {
        echo 'hello world'
    }
}
