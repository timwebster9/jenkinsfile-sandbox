node('mac') {

    stage('cleanup') {
        deleteDir()
    }

    stage('Checkout') {
        def scmInfo = checkout scm

        echo "Git URL: ${GIT_URL}"

        scmInfo.each { k,v ->
            echo "key: ${k}, value: ${v}"
        }
    }
}